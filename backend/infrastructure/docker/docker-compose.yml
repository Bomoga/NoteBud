version: "3.9"

services:
  db:
    image: neo4j:2025.01
    container_name: notebud-neo4j
    environment:
      NEO4J_AUTH: neo4j/notebud_password
      NEO4J_PLUGINS: '["apoc"]'
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
